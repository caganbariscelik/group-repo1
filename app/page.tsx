import { NeonMesh } from "@/components/ui/neon-mesh";

export default function HomePage() {
  return (
    <NeonMesh
      title="LOST & FOUND"
      subtitle="Campus Lost & Found"
      description="Report what you've lost, browse what's been found, and reconnect with your belongings across campus."
      className="-mx-4 -my-6 sm:-mx-6"
    />
  );
}
